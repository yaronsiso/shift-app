import 'package:flutter/material.dart';

/// Color tokens from the PRD design language (Part 2):
/// "רקע כהה מודרני (Dark Slate #0F172A) עם אלמנטים ניגודיים ויוקרתיים
/// (אזמרגד / זהב מוברש / לבן נקי)".
class AppColors {
  AppColors._();

  static const Color darkSlateBackground = Color(0xFF0F172A);
  static const Color surface = Color(0xFF1B2436); // slightly lifted card surface
  static const Color emerald = Color(0xFF10B981);
  static const Color brushedGold = Color(0xFFC9A227);
  static const Color cleanWhite = Color(0xFFF8FAFC);
  static const Color mutedText = Color(0xFF94A3B8);
  static const Color danger = Color(0xFFEF4444);
}
